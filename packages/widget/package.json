{
  "name": "@fruitback/widget",
  "version": "0.1.0",
  "description": "Visual feedback widget: click an element, write a note, get a Linear issue.",
  "license": "MIT",
  "files": [
    "dist",
    "src",
    "LICENSE",
    "THIRD-PARTY-NOTICES.md"
  ],
  "type": "module",
  "sideEffects": false,
  "main": "src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "publishConfig": {
    "main": "./dist/fruitback.mjs",
    "types": "./dist/public.d.ts",
    "exports": {
      ".": {
        "types": "./dist/public.d.ts",
        "import": "./dist/fruitback.mjs"
      },
      "./global": "./dist/fruitback.iife.js"
    },
    "access": "public"
  },
  "scripts": {
    "build": "node build.ts",
    "format": "oxfmt --check .",
    "format:fix": "oxfmt .",
    "icons:build": "node build-icons.ts",
    "lint": "oxlint .",
    "prepack": "pnpm build",
    "test": "node --test 'src/**/*.test.ts'",
    "test:watch": "node --test --watch 'src/**/*.test.ts'",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json"
  },
  "dependencies": {
    "@fruitback/shared": "workspace:*"
  },
  "devDependencies": {
    "@iconify-json/ph": "^1.2.2",
    "@types/node": "catalog:",
    "esbuild": "catalog:",
    "happy-dom": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "react-grab": "catalog:",
    "typescript": "catalog:"
  },
  "nx": {
    "targets": {
      "test": {
        "inputs": [
          "default",
          "^production",
          "{workspaceRoot}/README.md",
          "{workspaceRoot}/LICENSE",
          "{workspaceRoot}/packages/fruitback/**/*"
        ]
      }
    }
  }
}
